name: Runner Test

on:
  push:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, Windows, X64]

    steps:
      - name: Check runner
        shell: powershell
        run: |
          Write-Host "Runner is working"
          hostname
          Get-Date
          $PSVersionTable.PSVersion